function GetHWID()
--     local a = require("ffi") or error("Failed to require FFI, please make sure Allow unsafe scripts is enabled!", 2)
--     a.cdef [[
--     typedef long(__thiscall* get_file_time_t)(void* this, const char* pFileName, const char* pPathID);
--     typedef bool(__thiscall* file_exists_t)(void* this, const char* pFileName, const char* pPathID);
-- ]]
--     local b = a.typeof("void***")
--     local c = Utils.CreateInterface("filesystem_stdio.dll", "VBaseFileSystem011") or error(_debug and "Failed to get VBaseFileSystem011 interface" or "error", 2)
--     local d = a.cast(b, c) or error(_debug and "Failed to cast rawfilesystem to filesystem" or "error", 2)
--     local e = a.cast("file_exists_t", d[0][10]) or error(_debug and "Failed to cast file_exists_t" or "error", 2)
--     local f = a.cast("get_file_time_t", d[0][13]) or error(_debug and "Failed to cast get_file_time_t" or "error", 2)
--     local function g() for h = 65, 90 do local i = string.char(h) .. ":\\Windows\\Setup\\State\\State.ini" if e(d, i, "ROOT") then return i end end return nil end

--     ;local i = g() or error(_debug and "Failed to bruteforce system directory" or "error", 2)
--     local j = f(d, i, "ROOT") or error(_debug and "get_file_time failed" or "error", 2)
--     local k = j * 2;
--     return k;
    return 6969
end

local hwid = GetHWID()
local string = "sex"
local randomtext = {
    "Debug",
    "de_debug",
    "we do a little debugging",
    "Debugger",
    "sm_debug",
    "laff laff debug debug du hurensohn",
    "Approved By The FBI",
    "Why are you reading this?",
    "staymad",
    "sm_sitez",
    "Are orphans parented to nil???",
    "''give me the resolver for free duh''",
    "marco_1 - Today at 3:48 PM smok cock",
    "I Want Chlenix1337",
    "Ench - Today at 3:50 PM Nice cock bro!",
    "es0 fix glow pls",
    "EXODUS OWNER - Today at 4:05 PM yessir show cock",
    "https://filelist.ro/",
    "https://gamesense.is/",
    "YOOO Candice just Died :(",
    "Twinkle twinkle little star kill that nigga not so far",
    "you cant override cheat fuctions by using ffi bruh",
    "nl don't have api for resolver lmao",
    "bush did 911",
    "i love bbc(news)",
    "fatherless detected",
    "invest 5 dollars in exodus coin - 9 years ago",
    "xane exit scam",
    "SPDI - Today at 3:51 PM You can’t fck with me",
    "add Biddle#8678 on discord for free v-bucks",
    "-Fixed soufiw iq",
    "Aimjunkies resolver loaded successfully",
    "JZ REZOLVER*",
    "Dacota - Today at 10:59 PM i'am nl api creator",
    "successfully ratted by soufiw",
    "go to pornhub.com to activate free v-bucks generator",
    "(missed shot due to spead) SmoK fix this 'that would be an autostop lua' ",
    "gamesneeze - stop hitting 1ks",
    "15 second countdown activated",
    "VANTED — Today at 5:03 PM But they don’t know how to shot on the head",
    "doxbin.com",
    "Neverlose v3 loaded",
    "Sh4r3c4t Today 18:53: p resolver update",
    "fixed soufiw laziness by loading (Reslover.lua)",
    "download onetap v5",
	    'Ai cumva cheat-ul ala de la Llama?',
    '˜”°•.˜”°• 1 •°”˜.•°”˜',
	'efortless',
	'foaie verde castravete ti-am dat 1 prin perete',
	'cand pun ochiu in luneta o vad pe ma-ta pe mocheta',
	'Juan ',
	'foaie verde si-o spatula ti-am dat cap cu ma-ta-n pula',
	'phenomz0r.net ez taps $$$',
    'mama coaie am dat click manual zici ca sunt alien cosminel',
    '1 by esoterik bro',
    '1 by pleata lui darth',
    'foaie verde be the heda ti-ai luat tap de la wanheda',
	'fie viata cat de grea iti dau tap ce pula mea',
	'foaie verde butelie intra glontu in chelie',
	'hacker de sentimente sparg capuri si apartamente',
	'skeet in buzunare sa-i dau tap lui fleeekkk',
	'Priveste partea buna, macar ai dat 100 damage la peretele din spatele meu !',
	'ESC --> Help & Options --> How to Play',
	'1 1 1 VeRy NeRvOs hEaDsHoT 1 1 1',
	'Pt fetele gravide recomand skeet cu vitamine.',
        '1 tie 1 lui ma-ta',
        'Asa a dat si ma-ta ochii peste cum ai dat tu cu capul',
        'futu-ti mortii ma-tii sa-ti fut de taran',
        'Tragem pula in mamele voastre!!!',
        'Te-ai speriat? Eu da!',
		'ai capul ala zici ca e made in china',
			'da-te ca-mi bag pula',
	'ai gresit 1way-ul',
	'alo baiatu, te-ai pierdut ? iti bag waze-u ?',
	'Ai corpul ala zici ca e halba de bere.',
	'ai corpul ala zici ca e lumanare de botez.',
	'VeRy NeRvoS BaIm',
	'frumos peek',
	'stai jos caine',
	'Tu si Oana Roman ce mai stati cu burta pe afara.',
	'apas f pt tine <3',
	'scz eram cu legitbotu on',
	'rostogoli-mi-as pula-n ma-ta cum se rostogoleste crocodilu-n apa',
	'coaie ce corp ai zici ca esti desenat cu stanga',
	'uffya BaIm GoD $$',
	'foaie verde de cucuta hai la tata sa te futa',
	'aleluia ai luat muia .!.',
	'nice desync, esti cu eternity ?',
	'lol aveam resolver-ul off.',
	'Cel mai nervos baim din viata mea',
	'asta ajunge la war montage ms ms',
	'BaIm bAiM BaIm',
	'ceapa verde foaie iute uite baimu cum se duce',
	'Foaie verde praf de ciori iti iei baim pana mori',
	'Foaie verde si-o lamaie iti dau baim si iei muie',
	'foaie verde acadea ia cu baim in pula mea',
        'sunt haiduc cunoscut, iti dau pula la pascut',
        'sunt tac-tu, hai sa-ti dau lape',
        'ba..nu stiu cum sa-ti spun, dar...isi mai da ma-ta filme ca-i pompier de cand i-am pus pula peste umar?',
		'sa-ti pun pula pe piept ', 
		'hai back to canal boschetare',
		'hai la gratar sa-ti frig o muie',
		
	'zici ca esti raul2k la tras',
    'am auzit ca vinzi invite-uri, ramai injectat',
	'missed shot due to mortii ma-tii de newcomer prost',
	'sa te futa hattrick pana iti ies caramele pe cur',
	'stie popa ca umbli dezgropat',
    'Sincer eu incurajez avortul indiferent de varsta pe care o ai.',
	'daca iti fut una in piept iti aplauda umerii',
	'daca imi scot pula avorteaza toate gravidele',
	'tu esti baiat de baiat, te-a facut tactu cu un poponar',
	'sa-ti dea dumnezeu maini scurte si mancarimi in cur',
    's-ar fute ma-ta dar pula e la mine',
	'Vezi ca vine spre ma-ta un transport de turci',
	'Cate pule au spart-o pe ma-ta aseara ?',
	'daca iti fut una in piept iti aplauda umerii',
	'da-te-n pula mea mi s-a deconectat tastatura',
	'da-te-n pula mea mi-a sarit mouse-ul de pe birou',
    'era sa ma impiedic in pizda ma-tii de prost',
	'Sa-mi bag bilele alea grele in pizdocul lu ma-ta',
	'Sa-mi bag pula-n cripta ma-tii',
	'Sa o intrebi pe ma-ta daca o mai dor genunchi de aseara',
	'foaie verde si o saltea, drop decoy in pula mea',
	'daca iti pun pula pe umar zici ca bagi busteni in casa',
	'aolo eram cu configu de negev',
	'Tarami-as coaiele prin coliva ma-tii',
	'vrei nu vrei pula-n gura tot mio iei',
	'Iti fut o palma de iti pusca ochii ca sigurantele',
	'Cacate-ai pietre de cale ferata',
	'iar am cazut cu pula-n ma-ta',
	'Manca-mi-ai stuful de pe delta curului',
	'stii ca sunt un camatar dau la buci apoi dispar',
    'Iti bag pula in gura sa-ti daram maselele',
	'Sa mi-o vezi in ciorba',
	'Să-ți dau la muie până ajung cu călcâiele pe umerii tăi.',
        'Sa-mi trag pula peste ma-ta daca ma mai omori o singura data!',
        'Javra micaaaa, m-ai omorat sugea-mi-ai pula pana la sperma',
		'nici ma-ta nu te suporta bai sclavule',
		'sa nu dai rq futu-ti mortii ma-tii de bot',
		'ai media dupa sclavule',
		'sa o slobozesc pe ma-ta in gura',
}
local function randosex()
    return randomtext[Utils.RandomInt(1, #randomtext)]:gsub('\"', '')
end

function loadsex()
    local index = Menu.Combo("Main", "Index", { "v1", "v2" }, 1)
    index:SetVisible(false)
    local index2 = Menu.Switch("Main", "Index2", false)
    index2:SetVisible(false)
    local index3 = Menu.Combo("Main", "Index3", { "v1", "v2", "v3" }, 1)
    index3:SetVisible(false)
    local fired = 0
    local hit = 0
    local missed = 0
    -- rage functions
    local logo = "https://images.emojiterra.com/twitter/v14.0/512px/1f921.png"
    local logo_size = Vector2.new(35, 35)
    local image_get = Http.Get(logo)
    local seconds = 0 -- don't touch
    local minutes = 0 -- don't touch
    local hours = 0 -- don't touch
    local last_time = GlobalVars.realtime
    local logo_picture = Render.LoadImage(image_get, logo_size)
    local min = Vector.new()
    local max = Vector.new()
    local target_correct = true
    function calc_angle(src, dst)
        local vecdelta = Vector.new(dst.x - src.x, dst.y - src.y, dst.z - src.z)
        local angles = QAngle.new(math.atan2(-vecdelta.z, vecdelta:Length2D()) * 180.0 / math.pi, (math.atan2(vecdelta.y, vecdelta.x) * 180.0 / math.pi), 0.0)
        return angles
    end

    local function calc_shit(xdelta, ydelta)
        if xdelta == 0 and ydelta == 0 then
            return 0
        end

        return math.deg(math.atan2(ydelta, xdelta))
    end

    local function normalize_yaw(yaw)
        while yaw > 180 do yaw = yaw - 360 end
        while yaw < -180 do yaw = yaw + 360 end
        return yaw
    end

    local function get_enemies(players) -- 获取敌人列表
        local ctable = {}
        for pi = 1, #players do
            local enemy = players[pi]
            if enemy ~= EntityList.GetLocalPlayer() then
                if not enemy:IsTeamMate() then
                    if not enemy:IsDormant() then
                        table.insert(ctable, enemy)
                    end
                end
            end
        end
        return ctable
    end

    function get_helikopter() -- 获取离准星最近的玩家
        local players = get_enemies(EntityList.GetPlayers())
        if not players then return end
        if not EntityList.GetLocalPlayer() then return end
        local eye_pos = EntityList.GetLocalPlayer():GetEyePosition()
        local lx, ly, lz = eye_pos.x, eye_pos.y, eye_pos.z
        local view = EngineClient.GetViewAngles()
        local view_x, view_y, roll = view.pitch, view.yaw, view.roll

        local bestenemy = nil
        local fov = 180

        for i = 1, #players do
            local enemy = players[i];
            local cur_e = enemy:GetProp("m_vecOrigin")
            local cur_fov = math.abs(normalize_yaw(calc_shit(lx - cur_e.x, ly - cur_e.y) - view_y + 180))
            if cur_fov < fov then
                fov = cur_fov
                bestenemy = enemy
            end
        end
        return bestenemy
    end

    local function Menu_addon()
        if last_time > GlobalVars.realtime then
            last_time = GlobalVars.realtime
        end
        ooga = GlobalVars.realtime - last_time
        if ooga > 1 then
            last_time = GlobalVars.realtime
            seconds = seconds + 1
        end
        if seconds > 59 then
            seconds = 0
            minutes = minutes + 1
        elseif minutes > 59 then
            minutes = 0
            hours = hours + 1
        elseif hours > 59 then
            hours = 0
        end

        if (not Cheat.IsMenuVisible()) then return end
        local Menu_sz = Render.GetMenuSize()
        local Menu_pos = Render.GetMenuPos()
        local saiz = math.floor((Menu_sz.x + Menu_sz.y) / 93)
        text_size = Render.CalcTextSize("Pasted.lua || Current Version: 69 | Awesome security!", saiz)
        Render.BoxFilled(Vector2.new(Menu_pos.x, Menu_pos.y - 7), Vector2.new(Menu_pos.x + Menu_sz.x, Menu_pos.y - 50), Color.new(0.1, 0.1, 0.1, 0.8))
        Render.Image(logo_picture, Vector2.new(Menu_pos.x + 10, Menu_pos.y - 45), logo_size)
        Render.Text(string.format("Pasted.lua || Current Version: 69 | 'Awesome security!'"), Vector2.new(Menu_pos.x + Menu_sz.x / 2 - text_size.x / 2, Menu_pos.y - text_size.y * 2.3), Color.new(1.0, 1.0, 1.0, 1.0), saiz, true)


    end

    local print_index = "";
    -- rage functions
    local c_rage = {
        -- unfinished ragebot
        update_misses = function(shot)
            if shot.reason == 1 then
                missed = missed + 1
            end
        end,
        -- check shots ( will update soon )
        check_shots = function(event)
            if event:GetName() ~= "player_death" then return end

            local me = EntityList.GetLocalPlayer()
            local victim = EntityList.GetPlayerForUserID(event:GetInt("userid"))
            local attacker = EntityList.GetPlayerForUserID(event:GetInt("attacker"))

            if victim == attacker or attacker ~= me then return end
            -- reset shots if the player we missed at died
            missed = 0
        end,
        -- prefer head conditions
        prefer_head_condition = function()
            local hitboxes = {
                head = 0, -- head = 0
                neck = 1,
                pelvis = 2,
                stomach = 3,
                lower_chest = 4,
                chest = 5,
                upper_chest = 6,
                right_thigh = 7,
                left_thigh = 8,
                right_calf = 9,
                left_calf = 10,
                right_foot = 11,
                left_foot = 12,
                right_hand = 13,
                left_hand = 14,
                right_upper_arm = 15,
                right_forearm = 16,
                left_upper_arm = 17,
                left_forearm = 18,
            }
            -- weapons
            local weapons = {
                scout = 40,
                awp = 9,
            }
            -- get velocity function
            get_velocity = function(index)
                -- x
                x = index:GetProp("DT_BasePlayer", "m_vecVelocity[0]")
                -- y
                y = index:GetProp("DT_BasePlayer", "m_vecVelocity[1]")
                --z
                z = index:GetProp("DT_BasePlayer", "m_vecVelocity[2]")
                if x == nil then
                    return
                end
                -- calculate and complete velocity
                return math.sqrt(x * x + y * y + z * z)
            end

            -- get players
            if 1 < 2 then

                local players = EntityList.GetPlayers()
                -- check if the player is in pairs of players
                for i, player in pairs(players) do
                    -- get localplayer
                    local LocalPlayer = EntityList.GetClientEntity(EngineClient.GetLocalPlayer())
                    -- check if localplayer exists
                    if LocalPlayer == nil then
                        return
                    end
                    -- we dont want to run our ragebot featuers on our teammates
                    if not player:IsTeamMate() then
                        -- get player
                        local player = player:GetPlayer();
                        -- get local player
                        local get_local_player = EngineClient.GetLocalPlayer()
                        -- get local player from client entity
                        local local_player1 = EntityList.GetClientEntity(get_local_player)
                        -- get player local
                        local local_player = local_player1:GetPlayer()
                        -- check if we can fire bullet from our origin to enemy origin
                        local bullet = Cheat.FireBullet(local_player, local_player:GetProp("m_vecOrigin"), player:GetProp("m_vecOrigin"))
                        -- get index
                        local index = player:EntIndex();
                        -- get duck amount
                        local m_flDuckAmount = player:GetProp("DT_BasePlayer", "m_flDuckAmount")
                        -- get enemy health
                        local m_iHealth = player:GetProp("m_iHealth")
                        -- get velocity of player
                        local player_velocity = math.floor(get_velocity(player))
                        -- get player view angles
                        local view_angles = player:GetRenderAngles()
                        -- get active weapon of localplayer
                        local weapon = local_player:GetActiveWeapon()
                        -- getplayer pitch
                        local pitch = player:GetProp("m_angEyeAngles[0]");
                        -- no weapon return
                        if weapon == nil then
                            return
                        end
                        -- get weapon damage
                        local weapon_damage = weapon:GetWeaponDamage()
                        -- if the player is alive, is lethal and the active weapon is scout then run the code
                        if (m_iHealth > 0 and m_iHealth < weapon_damage) and weapon:GetProp("m_iItemDefinitionIndex") == 40 then
                            for j = 0, 1 and 7, 18 do -- only shoot body hitboxes not head or legs
                                RageBot.SetTargetPriority(index, 95) -- set target priority
                                print_index = "index: " .. index
                                RageBot.EnableHitbox(index, j, false) -- disable head and leg hitboxes
                            end
                        end
                        if missed >= 2 then
                            for j = 0, 1 and 7, 18 do -- only shoot body hitboxes not head or legs
                                RageBot.EnableHitbox(index, j, false) -- disable head and leg hitboxes
                            end
                        end
                        -- holding awp
                        if weapon:GetProp("m_iItemDefinitionIndex") == 9 then
                            for j = 2, 6 do -- prioritize body
                                RageBot.SetTargetPriority(index, 95) -- set target priority
                                RageBot.SetHitboxPriority(index, j, 95)
                            end
                        end
                    end
                    -- ::continue::
                end
            end
        end,
    }
    local da = randosex();
    local days = 99999
    local resdbg = Menu.Switch(da, "Resolver Debugger", false, "Shows you info on the resolver, making it easy to report bugs")
    local resvers = Menu.Combo(da, "Resolving Method", { "V1", "V2" }, 1)
    local rollres = Menu.Switch(da, "[BETA] Roll Resolver", false, "Tries to resolve roll")
    local rollvers = Menu.Combo(da, "Roll Resolving Method", { "AnimLayers", "BulletTrace", "HitboxCenter" }, 0)
    resvers:SetInt(index:GetInt())
    rollres:SetBool(index2:GetBool())
    rollvers:SetInt(index3:GetInt())
    subssex = Menu.Text(da, days .. " Days Remaining")
    local ffi = require 'ffi'
    ffi.cdef [[	
        typedef void*(__thiscall* get_client_entity_t_128481294812948)(void*, int);
    
        struct CCSGOPlayerAnimstate_128481294812948 {
            char pad[3];
            char m_bForceWeaponUpdate;
            char pad1[91];
            void* m_pBaseEntity;
            void* m_pActiveWeapon;
            void* m_pLastActiveWeapon;
            float m_flLastClientSideAnimationUpdateTime;
            int m_iLastClientSideAnimationUpdateFramecount;
            float m_flAnimUpdateDelta;
            float m_flEyeYaw;
            float m_flPitch;
            float m_flGoalFeetYaw;
            float m_flCurrentFeetYaw;
            float m_flCurrentTorsoYaw;
            float m_flUnknownVelocityLean;
            float m_flLeanAmount;
            char pad2[4];
            float m_flFeetCycle;
            float m_flFeetYawRate;
            char pad3[4];
            float m_fDuckAmount;
            float m_fLandingDuckAdditiveSomething;
            char pad4[4];
            float m_vOriginX;
            float m_vOriginY;
            float m_vOriginZ;
            float m_vLastOriginX;
            float m_vLastOriginY;
            float m_vLastOriginZ;
            float m_vVelocityX;
            float m_vVelocityY;
            char pad5[4];
            float m_flUnknownFloat1;
            char pad6[8];
            float m_flUnknownFloat2;
            float m_flUnknownFloat3;
            float m_flUnknown;
            float m_flSpeed2D;
            float m_flUpVelocity;
            float m_flSpeedNormalized;
            float m_flFeetSpeedForwardsOrSideWays;
            float m_flFeetSpeedUnknownForwardOrSideways;
            float m_flTimeSinceStartedMoving;
            float m_flTimeSinceStoppedMoving;
            bool m_bOnGround;
            bool m_bInHitGroundAnimation;
            float m_flTimeSinceInAir;
            float m_flLastOriginZ;
            float m_flLastOriginZ;
            float m_flHeadHeightOrOffsetFromHittingGroundAnimation;
        };
    ]]

    local entity_list = ffi.cast(ffi.typeof("void***"), Utils.CreateInterface("client.dll", "VClientEntityList003"))
    local get_client_entity = ffi.cast("get_client_entity_t_128481294812948", entity_list[0][3])

    -- Caching common functions
    local entity_get_local_player = EntityList.GetClientEntity(EngineClient.GetLocalPlayer())
    local ffi_cast = ffi.cast

    -- Magic numbers
    local start_falling_time = 0.359375
    local end_falling_time = 1.15625

    local falling_time = 0
    local simulated_server_jump_fall = 0

    -- I really don't like this part of the code
    local jumping = false
    local reset = false
    local reset2 = false
    local reset3 = false
    function brutemain()
        local players = EntityList:GetPlayers()

        for i, player in pairs(players) do
            if player:IsTeamMate() then return end
            enemy_entity = player:GetPlayer()
            enemy_player = enemy_entity;
            speed_x = enemy_player:GetProp("DT_BasePlayer", "m_vecVelocity[0]")
            speed_y = enemy_player:GetProp("DT_BasePlayer", "m_vecVelocity[1]")
            speed = math.sqrt(speed_x * speed_x + speed_y * speed_y)
            local enemyplayer = player:GetPlayer()
            if not enemyplayer then return end
            local bOnGround = bit.band(enemyplayer:GetProp("DT_BasePlayer", "m_fFlags"), bit.lshift(1, 0)) ~= 0

            if not bOnGround then
                reset = false
                -- ::continue::
            end
        end
    end

    -- I really don't like this part of the code
    function Round(num, dp)
        --[[
        round a number to so-many decimal of places, which can be negative, 
        e.g. -1 places rounds to 10's,  
        
        examples
            173.2562 rounded to 0 dps is 173.0
            173.2562 rounded to 2 dps is 173.26
            173.2562 rounded to -1 dps is 170.0
        ]] --
        local mult = 10 ^ (dp or 0)
        return math.floor(num * mult + 0.5) / mult
    end

    function logicmain()
        local players = EntityList:GetPlayers()

        for i, player in pairs(players) do
            if player:IsTeamMate() then return end
            if player:GetProp("m_lifeState") == true then return end
            if player:IsDormant() then return end
            local enemy_entity = player:GetPlayer()
            local enemyplayer = player:GetPlayer():EntIndex()
            local enemy_player = player
            if not enemyplayer then return end
            enemy_player:SetProp("m_flPoseParameter", 1, 6)

            local player_ptr = ffi_cast("void***", get_client_entity(entity_list, enemyplayer))
            local animstate_ptr = ffi_cast("char*", player_ptr) + 0x9960
            local animstate = ffi_cast("struct CCSGOPlayerAnimstate_128481294812948**", animstate_ptr)[0]

            enemy_player = enemy_entity;
            speed_x = enemy_player:GetProp("DT_BasePlayer", "m_vecVelocity[0]")
            speed_y = enemy_player:GetProp("DT_BasePlayer", "m_vecVelocity[1]")
            speed = math.sqrt(speed_x * speed_x + speed_y * speed_y)
            local enemyplayer = player:GetPlayer()
            if not enemyplayer then return end
            local bOnGround = bit.band(enemyplayer:GetProp("DT_BasePlayer", "m_fFlags"), bit.lshift(1, 0)) ~= 0
            local in_hit_ground_animation = animstate.m_bInHitGroundAnimation
            local head_height_or_offset_from_hitting_ground_animation = animstate.m_flHeadHeightOrOffsetFromHittingGroundAnimation

            if in_hit_ground_animation then
                if head_height_or_offset_from_hitting_ground_animation and jumping and not reset then
                    entity_set_prop(enemyplayer, 'm_flPoseParameter', 1, 12)
                end
            end
        end
    end

    function brutemove()
        local players = EntityList:GetPlayers()

        for i, player in pairs(players) do
            if player:IsTeamMate() then return end
            enemy_entity = player:GetPlayer()
            enemy_player = enemy_entity;
            speed_x = enemy_player:GetProp("DT_BasePlayer", "m_vecVelocity[0]")
            speed_y = enemy_player:GetProp("DT_BasePlayer", "m_vecVelocity[1]")
            speed = math.sqrt(speed_x * speed_x + speed_y * speed_y)
            if speed < 2 then return end
            local enemyplayer = player:GetPlayer()
            if not enemyplayer then return end
            local bOnGround = bit.band(enemyplayer:GetProp("DT_BasePlayer", "m_fFlags"), bit.lshift(1, 0)) ~= 0

            if not bOnGround then
                reset2 = false
                -- ::continue::
            end
        end
    end

    function bruteair()
        local players = EntityList:GetPlayers()

        for i, player in pairs(players) do
            if player:IsTeamMate() then return end
            enemy_entity = player:GetPlayer()
            enemy_player = enemy_entity;
            speed_x = enemy_player:GetProp("DT_BasePlayer", "m_vecVelocity[0]")
            speed_y = enemy_player:GetProp("DT_BasePlayer", "m_vecVelocity[1]")
            local on_ground = bit.band(enemy_player:GetProp('m_fFlags'), 1) ~= 0
            speed = math.sqrt(speed_x * speed_x + speed_y * speed_y)
            if on_ground then return end
            local enemyplayer = player:GetPlayer()
            if not enemyplayer then return end
            local bOnGround = bit.band(enemyplayer:GetProp("DT_BasePlayer", "m_fFlags"), bit.lshift(1, 0)) ~= 0

            if not bOnGround then
                reset3 = false
                -- ::continue::
            end
        end
    end

    -- I really don't like this part of the code
    function Round(num, dp)
        --[[
        round a number to so-many decimal of places, which can be negative, 
        e.g. -1 places rounds to 10's,  
        
        examples
            173.2562 rounded to 0 dps is 173.0
            173.2562 rounded to 2 dps is 173.26
            173.2562 rounded to -1 dps is 170.0
        ]] --
        local mult = 10 ^ (dp or 0)
        return math.floor(num * mult + 0.5) / mult
    end

    function logicmove()
        local players = EntityList:GetPlayers()

        for i, player in pairs(players) do
            if player:IsTeamMate() then return end
            if player:GetProp("m_lifeState") == true then return end
            if player:IsDormant() then return end
            local enemy_entity = player:GetPlayer()
            local enemyplayer = player:GetPlayer():EntIndex()
            local enemy_player = player
            if not enemyplayer then return end
            enemy_player:SetProp("m_flPoseParameter", 1, 6)

            local player_ptr = ffi_cast("void***", get_client_entity(entity_list, enemyplayer))
            local animstate_ptr = ffi_cast("char*", player_ptr) + 0x9960
            local animstate = ffi_cast("struct CCSGOPlayerAnimstate_128481294812948**", animstate_ptr)[0]

            enemy_player = enemy_entity;
            speed_x = enemy_player:GetProp("DT_BasePlayer", "m_vecVelocity[0]")
            speed_y = enemy_player:GetProp("DT_BasePlayer", "m_vecVelocity[1]")
            speed = math.sqrt(speed_x * speed_x + speed_y * speed_y)
            local enemyplayer = player:GetPlayer()
            if not enemyplayer then return end
            local bOnGround = bit.band(enemyplayer:GetProp("DT_BasePlayer", "m_fFlags"), bit.lshift(1, 0)) ~= 0
            if speed < 2 then return end
            local in_hit_ground_animation = animstate.m_bInHitGroundAnimation
            local head_height_or_offset_from_hitting_ground_animation = animstate.m_flHeadHeightOrOffsetFromHittingGroundAnimation

            if in_hit_ground_animation then
                if head_height_or_offset_from_hitting_ground_animation and jumping and not reset2 then
                    entity_set_prop(enemyplayer, 'm_flPoseParameter', 1, 12)
                    -- ::continue::
                end
            end
        end
    end

    function logicair()
        local players = EntityList:GetPlayers()

        for i, player in pairs(players) do
            if player:IsTeamMate() then return end
            if player:GetProp("m_lifeState") == true then return end
            if player:IsDormant() then return end
            local enemy_entity = player:GetPlayer()
            local enemyplayer = player:GetPlayer():EntIndex()
            local enemy_player = player
            if not enemyplayer then return end
            enemy_player:SetProp("m_flPoseParameter", 1, 6)

            local player_ptr = ffi_cast("void***", get_client_entity(entity_list, enemyplayer))
            local animstate_ptr = ffi_cast("char*", player_ptr) + 0x9960
            local animstate = ffi_cast("struct CCSGOPlayerAnimstate_128481294812948**", animstate_ptr)[0]

            enemy_player = enemy_entity;
            speed_x = enemy_player:GetProp("DT_BasePlayer", "m_vecVelocity[0]")
            speed_y = enemy_player:GetProp("DT_BasePlayer", "m_vecVelocity[1]")
            speed = math.sqrt(speed_x * speed_x + speed_y * speed_y)
            local enemyplayer = player:GetPlayer()
            if not enemyplayer then return end
            local bOnGround = bit.band(enemyplayer:GetProp("DT_BasePlayer", "m_fFlags"), bit.lshift(1, 0)) ~= 0
            if bOnGround then return end
            local in_hit_ground_animation = animstate.m_bInHitGroundAnimation
            local head_height_or_offset_from_hitting_ground_animation = animstate.m_flHeadHeightOrOffsetFromHittingGroundAnimation

            if in_hit_ground_animation then
                if head_height_or_offset_from_hitting_ground_animation and jumping and not reset3 then
                    entity_set_prop(enemyplayer, 'm_flPoseParameter', 1, 12)
                    -- ::continue::
                end
            end
        end
    end

    local yaw2 = 0
    Resolvedetectsimple = function()
        local players = EntityList:GetPlayers()
        for i, player in pairs(players) do
            if not player:IsTeammate() and player:GetProp("m_lifeState") ~= true and not player:IsDormant() then
                local pitch = player:GetProp("m_angEyeAngles[0]");
                local name = player:GetName()
                local pitch = player:GetProp("m_angEyeAngles[0]");
                if pitch < 33 then
                    if pitch == 0 then
                        yaw2 = player:GetProp("m_angEyeAngles[1]");
                    else
                        yaw2 = 0
                    end
                else
                    yaw2 = player:GetProp("m_angEyeAngles[1]");
                end
                local name = player:GetName()
                --	print(string.format("%s, pitch: %s", name, pitch))
                if pitch > 82 and pitch < 89 then
                    return "Resolved"
                else
                    return "Resolving"
                end
                    
            end
            -- if player:IsTeamMate() then goto continue end
            -- if player:GetProp("m_lifeState") == true then goto continue end
            -- if player:IsDormant() then goto continue end

            -- ::continue::
        end
    end
    Resolvedetectdetailed = function()
        local players = EntityList:GetPlayers()
        for i, player in pairs(players) do
            if not player:IsTeammate() and player:GetProp("m_lifeState") ~= true and not player:IsDormant() then
                local pitch = player:GetProp("m_angEyeAngles[0]");
                local name = player:GetName()
                local pitch = player:GetProp("m_angEyeAngles[0]");
                yaw2 = player:GetProp("m_angEyeAngles[1]");
                local name = player:GetName()
                --	print(string.format("%s, pitch: %s", name, pitch))
                if pitch > 82 and pitch < 89 then
                    return string.format("X: %s, Y: %s", Round(math.min(yaw2 / 3, 58), 2), math.floor(pitch))
                else
                    return ""
                end
            end
            -- if player:IsTeamMate() then goto continue end
            -- if player:GetProp("m_lifeState") == true then goto continue end
            -- if player:IsDormant() then goto continue end

            -- ::continue::
        end
    end
    local yaw = 0
    local fontes = Render.InitFont("Verdana", 24)
    Cheat.RegisterCallback("draw", function()
        Menu_addon()
        index:SetInt(resvers:GetInt())
        index2:SetBool(rollres:GetBool())
        index3:SetInt(rollvers:GetInt())
        if resdbg:GetBool() then
            local players = EntityList:GetPlayers()
            local caca = 0
            for i, player in pairs(players) do
                if not player:IsTeamMate() and player:GetProp("m_lifeState") ~= true and not player:IsDormant() then
                    local col1 = math.floor(math.sin(GlobalVars.realtime * 0.5) * 200 + 250) -- R
                    local col2 = math.floor(math.sin(GlobalVars.realtime * 0.5 + 3) * 200 + 250) -- G
                    local col3 = math.floor(math.sin(GlobalVars.realtime * 0.5 + 5) * 200 + 250) -- B
                    yaw = player:GetProp("m_angEyeAngles[1]");
                    pitch = player:GetProp("m_angEyeAngles[0]");
                    local name = player:GetName()
                    caca = caca + 30
                    Render.Text(string.format("%s, X: %s, Y: %s", name, Round(math.min(yaw / 3, 58), 1), math.floor(pitch)), Vector2.new(25, 400 + caca), Color.new(col1 / 255, col2 / 255, col3 / 255, 1), 24, fontes)
                end
                -- if player:IsTeamMate() then goto continue end
                -- if player:GetProp("m_lifeState") == true then goto continue end
                -- if player:IsDormant() then goto continue end
                -- ::continue::
            end
        end
    end)
    ESP.CustomText("Resolver.lua Simple", "enemies", "Resolved", function(ent)
        return Resolvedetectsimple()
    end)
    ESP.CustomText("Resolver.lua Detailed", "enemies", "X: 0, Y: 0", function(ent)
        return Resolvedetectdetailed()
    end)
    Cheat.RegisterCallback('events', function(event)
        if missed ~= 0 then
            reset = true
            reset2 = true
            reset3 = true
        end
        -- print(tostring(reset) .. "; " .. tostring(reset2) .. "; " .. tostring(reset3) .. "; " .. tostring(missed))
    end)
    Cheat.RegisterCallback('createmove', function()
        if resvers:GetInt() == 0 then
            brutemain()
            brutemove()
            bruteair()
            logicmain()
            logicmove()
            logicair()
        else
            brutemain()
            logicmain()
        end
    end)
    Cheat.RegisterCallback('pre_prediction', function()
        if get_helikopter() and rollres:GetBool() then
            if rollvers:GetInt() == 0 then
                RageBot.EnableMultipoints(get_helikopter():EntIndex(), 0, false)
                RageBot.ForceHitboxSafety(get_helikopter():EntIndex(), 0)
            elseif rollvers:GetInt() == 2 then
                RageBot.ForceSafety(get_helikopter():EntIndex())
            else
                RageBot.EnableMultipoints(get_helikopter():EntIndex(), 0, false)
            end
        end
        if resvers:GetInt() == 0 then
            brutemove()
            logicmove()
        end
    end)
    Cheat.RegisterCallback('frame_stage', function()
        if resvers:GetInt() == 0 then
            bruteair()
            logicair()
        end
    end)
    Cheat.RegisterCallback('registered_shot', function(shot)
        c_rage.update_misses(shot)
        local reason = shot.reason
    end)
    print('         √ Succesfully Loaded SHIT.LUA')
    print('               Welcome, ' .. Cheat:GetCheatUserName() .. "!")
    print('         LAST UPDATED: 4/11/2022')
    -- Http.Get(string.format("https://exodus.wtf/getloggedbitch.php?name=%s&lual=%s&succes=%s", Cheat.GetCheatUserName():gsub("%#", "_"), "resolver", "true"))
end

days = 99999
hwidcloud = hwid
EngineClient.ExecuteClientCmd("clear")
-- print('██████╗ ███████╗███████╗ ██████╗ ██╗    ██╗   ██╗███████╗██████╗    ██╗     ██╗   ██╗ █████╗ ')
-- print('██╔══██╗██╔════╝██╔════╝██╔═══██╗██║    ██║   ██║██╔════╝██╔══██╗   ██║     ██║   ██║██╔══██╗')
-- print('██████╔╝█████╗  ███████╗██║   ██║██║    ██║   ██║█████╗  ██████╔╝   ██║     ██║   ██║███████║')
-- print('██╔══██╗██╔══╝  ╚════██║██║   ██║██║    ╚██╗ ██╔╝██╔══╝  ██╔══██╗   ██║     ██║   ██║██╔══██║')
-- print('██║  ██║███████╗███████║╚██████╔╝███████╗╚████╔╝ ███████╗██║  ██║██╗███████╗╚██████╔╝██║  ██║')
-- print('╚═╝  ╚═╝╚══════╝╚══════╝ ╚═════╝ ╚══════╝ ╚═══╝  ╚══════╝╚═╝  ╚═╝╚═╝╚══════╝ ╚═════╝ ╚═╝  ╚═╝')
print('Owned by SmoK HvH')
if days <= 0 then
    -- Http.Get(string.format("https://exodus.wtf/getloggedbitch.php?name=%s&lual=%s&succes=%s", Cheat.GetCheatUserName():gsub("%#", "_"), "resolver", "false"))
    print('         X Failed To Load RESOLVER')
    print('               No License')
elseif days > 0 then
    if hwidcloud == 000000 then
        -- Http.Get("https://exodus.wtf/bard/add_hwid.php?user=" .. Cheat.GetCheatUserName():gsub("%#", "_") .. "&hwid=" .. hwid)
        loadsex()
    elseif hwidcloud ~= hwid then
        -- Http.Get(string.format("https://exodus.wtf/getloggedbitch.php?name=%s&lual=%s&succes=%s", Cheat.GetCheatUserName():gsub("%#", "_"), "resolver", "false"))
        print('         X Failed To Load RESOLVER')
        print('               Invalid HWID')
    else
        loadsex()
        -- ::continue::
    end
end
